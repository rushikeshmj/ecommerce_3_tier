apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-gateway.fullname" . | replace "api-gateway", .Chart.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Chart.Name }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
